HeapPoint = cust {
    {x} int;
    init = (self) function {
        self.x = 1;
        return 0;
    } int;
};

main = () function {
    {p} adr = rent(1, HeapPoint);
    p.init();
    {a} int = moveOut(p);
    {b} int = moveOut(p);
    return a + b;
} int;

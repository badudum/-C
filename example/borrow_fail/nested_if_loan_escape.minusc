main = () function {
    {buf} adr = rent(16);
    {bad} int = 0;
    if (Real) {
        if (Real) {
            {r} &adr = buf;
            bad = PeekInt(r, 0);
        };
        {worse} int = PeekInt(r, 0);
    };
    return 0;
} int;

main = () function {
    {buf} adr = rent(16);
    if (Real) {
        {fd} int = moveOut(buf);
    } else {
        {v} int = PeekInt(buf, 0);
    };
    {bad} int = PeekInt(buf, 0);
    return 0;
} int;
